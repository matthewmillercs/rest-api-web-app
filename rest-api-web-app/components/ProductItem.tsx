import React from "react";
import { Box, Card } from "@mui/material";

export const ProductItem = () => {
    return (
        <Card sx={{ height: 300, width: 300, backgroundColor: "orange" }}>
            <Box></Box>
        </Card>
    );
};
